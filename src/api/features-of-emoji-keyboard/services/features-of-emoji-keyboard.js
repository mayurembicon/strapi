'use strict';

/**
 * features-of-emoji-keyboard service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::features-of-emoji-keyboard.features-of-emoji-keyboard');
