'use strict';

/**
 *  features-of-emoji-keyboard controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::features-of-emoji-keyboard.features-of-emoji-keyboard');
