'use strict';

/**
 * features-of-emoji-keyboard router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::features-of-emoji-keyboard.features-of-emoji-keyboard');
