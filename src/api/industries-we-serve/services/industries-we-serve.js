'use strict';

/**
 * industries-we-serve service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::industries-we-serve.industries-we-serve');
