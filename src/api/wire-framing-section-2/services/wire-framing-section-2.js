'use strict';

/**
 * wire-framing-section-2 service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::wire-framing-section-2.wire-framing-section-2');
