'use strict';

/**
 * formula-car-section-1 service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::formula-car-section-1.formula-car-section-1');
